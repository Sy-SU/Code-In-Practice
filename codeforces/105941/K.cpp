#include <bits/stdc++.h>

using i64 = long long;

constexpr int N = 2e5 + 10;

int cnt[] = {1, 0, 0, 0, 1, 0, 1, 0, 2, 1};

void solve() {
	int n, m;
	std::cin >> n >> m;

	std::vector<int> a(n + 1);
	for (int i = 1; i <= n; i++) {
		std::cin >> a[i];
	}


}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	solve();

	return 0;
}