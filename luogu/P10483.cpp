#include <bits/stdc++.h>

using i64 = long long;

constexpr int N = 2e5 + 10;

void solve() {
	int n, w;
	std::cin >> n >> w;

	std::vector<int> c(n + 1, 0);
	for (int i = 1; i <= n; i++) {
		std::cin >> c[i];
	}

	
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	solve();

	return 0;
}