#include <bits/stdc++.h>

using i64 = long long;

constexpr int N = 2e5 + 10;
constexpr double eps = 1e-4;

void solve() {
	double w0, w, m;
	std::cin >> w0 >> w >> m;

	double lo = 0, hi = 300;
	while (hi - lo >= eps) {

	}
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	solve();

	return 0;
}