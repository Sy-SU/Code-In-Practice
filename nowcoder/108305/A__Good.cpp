#include <bits/stdc++.h>

using i64 = long long;

void solve() {
	std::string x;
	std::cin >> x;

	if (x[0] == '-') {
		std::cout << '-';
		for (int i = 1; i < (int)x.size(); i++) {
			if (x[i])
		}
	} else {

	}
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);

	int t = 1;
	std::cin >> t;
	while (t--) {
		solve();
	}

	return 0;
}